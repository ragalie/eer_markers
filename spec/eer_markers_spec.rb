require 'spec_helper'

describe EerMarkers do
  it 'has a version number' do
    expect(EerMarkers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
